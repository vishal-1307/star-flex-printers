---
import { gallery } from "../data/site";
---

<section id="gallery" class="halftone mx-auto max-w-6xl px-4 py-16 sm:py-20">
  <div class="reveal mb-10 max-w-xl">
    <p class="mb-2 text-sm font-semibold uppercase tracking-wide text-accent">Hamara Kaam</p>
    <h2 class="font-display text-3xl font-bold text-ink sm:text-4xl">
      Kaam ki <span class="marker">Jhalak</span>
    </h2>
    <p class="mt-3 text-ink/70">
      Neeche placeholder tasveerein hain — asli kaam ki photos jald jodi jayengi.
    </p>
  </div>

  <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4">
    {gallery.map((file, i) => (
      <div class="reveal cropmarks overflow-hidden rounded-lg border border-stone-200 bg-white">
        <img
          src={`/gallery/${file}`}
          alt={`Star Flex Printers ka kaam — placeholder photo ${i + 1}`}
          class="aspect-square w-full object-cover"
          loading="lazy"
          width="800"
          height="600"
        />
      </div>
    ))}
  </div>
</section>
